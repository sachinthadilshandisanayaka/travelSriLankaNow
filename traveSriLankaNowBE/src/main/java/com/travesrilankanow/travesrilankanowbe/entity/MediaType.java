package com.travesrilankanow.travesrilankanowbe.entity;

/**
 * Enum representing different types/usages of media files in the system.
 * Used for categorizing and filtering images based on their purpose.
 */
public enum MediaType {
    HERO_SLIDE,
    PAGE_HEADER,
    LOCATION,
    LOCATION_GALLERY,
    EVENT,
    PLACE,
    PLACE_GALLERY,
    GALLERY,
    GENERAL
}

# Non-secret client config — safe to commit to git
# Copy this folder to clients/PROJXXX/ when adding a new client
# Then create Jenkins credential: PROJXXX-secrets (Secret File with the .env contents)

SERVER_IP=root@YOUR_SERVER_IP
DOMAIN=www.clientdomain.com
SSH_CRED_ID=ssh-key-projXXX

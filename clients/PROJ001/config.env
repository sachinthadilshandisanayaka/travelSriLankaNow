# Non-secret client config — safe to commit to git
# Secrets (DB password, JWT, MinIO keys) live in Jenkins → Credentials → PROJ001-secrets

SERVER_IP=root@169.58.12.239
DOMAIN=169.58.12.239
SSH_CRED_ID=ssh-key-proj001

# Non-secret client config — safe to commit to git
# Secrets (DB password, JWT, MinIO keys) live in Jenkins → Credentials → PROJ001-secrets

SERVER_IP=root@169.58.12.239
DOMAIN=ruklaktravels.com
SSH_CRED_ID=ssh-key-proj001
NGINX_CONF=nginx-ssl

# Branding — used to patch index.html OG/Twitter meta tags at deploy time
SITE_NAME=Rukla Travels
SITE_TAGLINE=Your next story starts here
SITE_DESCRIPTION=Explore Sri Lanka travel packages, day tours, events and destinations with Rukla Travels.

# Theme — see travelSrilankaNow/generate-theme.sh for available presets (navy, warm, gold)
THEME_PRESET=warm

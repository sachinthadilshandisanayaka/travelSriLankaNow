version: '3.8'

services:
  backend:
    ports:
      - "8080:8080"

  frontend:
    ports:
      - "4200:80"

  nginx-proxy:
    profiles:
      - production

  certbot:
    profiles:
      - production
